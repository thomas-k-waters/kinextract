#!/bin/bash
set +o noclobber

# Source the SCO environment configuration
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${script_dir}/sco_env.sh"

rm -f b??????

python "${SCO_FRAMEWORK_BIN}/makebins_stis.py"

for b in b??????; do chmod +x $b; done
